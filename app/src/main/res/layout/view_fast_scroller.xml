<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android">

    <View
        android:id="@+id/fastscroll_dim"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#99000000"
        android:visibility="gone" />

    <View
        android:id="@+id/fastscroll_handle"
        android:layout_width="5dp"
        android:layout_height="70dp"
        android:layout_gravity="end"
        android:layout_marginEnd="0dp"
        android:background="@drawable/fastscroll_handle_bg" />

    <TextView
        android:id="@+id/fastscroll_center_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textColor="@color/white"
        android:textSize="90sp"
        android:textStyle="bold"
        android:visibility="gone"
        android:shadowColor="#000000"
        android:shadowDx="4"
        android:shadowDy="4"
        android:shadowRadius="10" />

    <!-- Hidden dummy to maintain compatibility if needed -->
    <TextView
        android:id="@+id/fastscroll_bubble"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

</merge>